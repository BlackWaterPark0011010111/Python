Matplotlib:
           Pyplot
           Plotting
           Markers
           Line
           Labels
           Grid
           Subplot
           Scatter
           Bars
           Histograms
           Pie Charts  


PYPLOT:

изначально пакет pyplot- это и есть основная лошадка,которая и запускает
все в matplotlib.там те функции которые отвечают за отображение всех
графиков.
обращаемся к ветке pyplot->
matplotlib.pyplot -> import matplotlib.pyplot as plt

```
указываем координаты на оси[1,4,8,6,9] 
->plt.plot([1,4,8,6,9])  
```  
и     
plt.show()-чтобы увидеть график
show()->указывает программе не закрывать окно.если ее не ввести,окно графика
откроется но настолько же быстро и закроется.потому что команда show
фиксирует отображение графика на экране.
мы также можем использовать библиотеку numpy,и код будет выглядеть проще
например без numpy:
```
import matplotlib.pyplot as plt
plt.plot([1,4,8,6,9])
plt.show()
```
и с numpy:
import numpy as np
и тут мы сразу устанавливаем значения по оси 'Y'->
```
y=np.array([1,4,8,6,9]) --> точки по координате 'Y'
plt.plot(y)
plt.show()
```
когда мы в plot передавали  массив данных без использования numpy,то там
сначала происходила конвертация  с использованием библиотеки numpy, и только
потом эти данные отображались,а можно сразу прописывать импорт numpy и
писать координаты для оси через numpy. И вообще почти все функции пакета
matplotlib работают через библиотеку numpy.
когда мы передаем значения по оси координат 'Y', то x автоматически
приобретает значения от 0 и до ......
НАПРИМЕР:
Х  0  1  2  3  4 --выставляются автоматически,даже если мы их не указали
Y  1  2  9  5  4
И ЕСЛИ УКАЗЫВАЕМ:
```
x=np.array([1,4,5,8,7])
y=np.array([1,4,8,6,9])
plt.plot(x,y)-- и если мы в plot, перелаем 2 аргумента x и y,то они
передаются только так,через запятую.
plt.show()
```
ЛИБО:
мы прописываем значения для Y от 0 до 4:
```y=np.arange(0,5,1)  -->[0, 1, 2, 3, 4]  с помощью arange  с лева на право
движение вперед,вверх
np.arange(5, 0, -1) ---с права на лево,движение назад,вниз 
шаг 1
```
``` 
x=np.array([a*a for a in y]) --> [0,1,4,9,16]
```
для оси x- он будет меняться от 0 и до 16,то есть мы значения Y возводим в
квадрат и будет получен список для X.
Чтобы увидеть сетку --- grid ---> plt.grid()
чтобы увидеть 2 графика,можно добавить 2 коллекции:
```
x1=[1,4,5,8,7]
y1=[a*a for a in y]
```
И
```
x2=[0,1,2,3]
y2=[i+1 for i in y2]
plt.plot(x1,y1,x2,y2)
plt.show()
```
В matplotlib-нет согласованности данных и все будет показано так как
согласовано.
plt.plot(x1,y1,'--') ---> 
'--' -означает что график будем видеть штриховой,сам тип линии при
отображение графика.
'-'   ---> непрерывная линия(по умолчанию)  plt.plot(x1,y1,'-')
'--'  --->  штриховая линия  plt.plot(x1,y1,'--')
'-.'  --->  штрихпунктирная линия   plt.plot(x1,y1,'-.')
':'   --->  пунктирная линия    plt.plot(x1,y1,':')
'None' или '' ---> без рисования линия (вообще ничего не увидим)plt.plot(x1,y1,None(''))

НО!

у нас есть график и он уже в 2D. и мы можем управлять этим отображением
НАПРИМЕР:
lines=plt.plot(x,y,':')
plt.setp(lines,linestyle=':')
есть специальная функция которая позволяет установить специальные свойства "SETP"  и нам нужно прописать в plt так же эту функцию со специальным
именованным параметром "linestyle". и так мы можем менять внешний вид линий и если мы передаем в LINES графики  x1,y1,x2,y2,x3,y3 --- то все они будут
такими которыми мы задали в linestyle.
ТАКЖЕ мы можем менять их цвет------------

🔵	'b'	blue	
🟢	'g'	green	 
🔴	'r'	red
🟡	'y'	yellow	
⚫	'k'	black
⚪	'w'	white 
🎨	'c'	cyan  
🟣	'm'	magen 
plt.plot(x, y, 'ro')     ----> красные кружки
plt.plot(x, y, 'b--',x2, y2,'k:')    ----> синяя штриховая линия
plt.plot(x, y, 'g^-')    ----> зеленые треугольники со сплошной линией
ЧЕРЕЗ НАЗВАНИЯ:
plt.plot(x, y, color='red')
plt.plot(x, y, color='steelblue')
                 HEX код
🔵	'blue'	    #0000FF
🟢	'green'	    #008000
🔴	'red'	    #FF0000
🟡	'yellow'	#FFFF00
🟠	'orange'	#FFA500
🟣	'purple'	#800080
🎨	'cyan'	    #00FFFF
🌸	'pink'	    #FFC0CB
🟤	'brown'	    #A52A2A
🔶	'coral'	    #FF7F50
�️	'gold'	     #FFD700
🌿	'lime'	    #00FF00
🔷	'navy'	    #000080
🫐	'teal'	    #008080
🍇	'violet'	#EE82EE

ПРИМЕР:

plt.plot(x, y, color='#FF5733')
plt.plot(x, y, color='#33FF57')


RGB-кортеж:....................................................................................................
plt.plot(x, y, color=(0.1, 0.2, 0.5))  -----> значения от 0 до 1
plt.plot(x, y, color=(255, 0, 0))  


МАРКЕРЫ....................................................................................................
есть именованный параметр ---> marker=''
lines=plt.plot(x,y 'r--o',marker='s',markerfacecolor='w')
lines=plt.plot(оси,цвет и фигуры на штрихе , цвет точки x и y,толщина линии(linewidth=4) )

●	'.'	точка (маленькая)
◆	','	пиксель (очень маленький)
◯	'o'	круг
+	'+'	плюс
✕	'x'	крестик
△	'^'	треугольник вверх
▽	'v'	треугольник вниз
◁	'<'	треугольник влево
▷	'>'	треугольник вправо
☆	'*'	звезда
□	's'	квадрат
◇	'D'	ромб (diamond)
▷◁	'd'	тонкий ромб
▼	'p'	пятиугольник (pentagon)
⬡	'h'	шестиугольник (hexagon)
✶	'H'	шестиугольник (другой тип)
─	'_'	горизонтальная линия
│	'|'	вертикальная линия
◉	'None' или ' '	без маркера
◎	'none'	без маркера


Маркеры из LaTeX....................................................................................................

$\star$	'*'	звезда
$\dagger$	'1'	кинжал
$\ddagger$	'2'	двойной кинжал
$\diamond$	'3'	ромб
$\triangledown$	'4'	треугольник вниз


Числовые коды маркеров....................................................................................................

'0'	треугольник (tickleft)
'1'	треугольник (tickright)
'2'	треугольник (tickup)
'3'	треугольник (tickdown)
'4'	карет (caret)
'8'	восьмиугольник

plt.plot(x, y, 'ro-')   ----> красные кружки со сплошной линией
plt.show()
plt.plot(x, y, 'b^--')  ----> синие треугольники с штриховой линией
plt.show()
plt.plot(x, y, 'gs:')   ----> зеленые квадраты с пунктирной линией
plt.show()


Заливка областей графика..................................................................................................

fill_between(x,y1,y2=0,where=None,interpolate=False,step=None,*,data=None,**kwargs)
(первая кривая относительно которой делается заливка, вторая кривая,)

```
x=np.arange(-2*np.pi,2*np.pi,0.1)
y=np.cos(x)
plt.plot(x,y)
plt.fill_between(x,y,0.5)  ИЛИ
plt.fill_between(x,y,where(y < 0),color='r', alpha=0.5)
plt.fill_between(x,y,where(y > 0),color='k', alpha=0.5)
plt.grid()
plt.show()

```


Отображение нескольких графиков в одном окне .............................................................................

Благодаря функции    SUBPLOT
subplot(nrows,ncols,index)
nrows,ncols ==число строк и столбцов
index== индекс текущих координатных осей
import numpy as np
import matplotlib.pyplot as plt
plt.subplot(1,3,1) <--1 
plt.plot(np.random.random(10))
plt.subplot(1,3,2) <--2 
plt.plot(np.random.random(10))
plt.subplot(1,3,3) <--3
plt.plot(np.random.random(10))
plt.grid()   <---сетка
plt.show

3 раза вызывается subplot, где создается ось с координатами,и в каждых осях
отображается график со случайными значениями
но мы тут прописываем grid только после самой последней оси и применятся она
будет только к последней,чтобы сетка была видна в каждом графике:
мы помещаем координаты в переменную и потом дописываем к ней grid()


```
ax1=plt.subplot(1,3,1) <--1 
plt.plot(np.random.random(10))
ax2=plt.subplot(1,3,2) <--2 
plt.plot(np.random.random(10))
ax3=plt.subplot(1,3,3) <--3
plt.plot(np.random.random(10))
ax1.grid()
ax2.grid()
ax3.grid()  
plt.show
```


ТОЧКИ....................................................................................................

Нанесение и оформление отдельных точек функцией scatter()
Для нанесения на диаграмму отдельной точки используется функция scatter(). 
мы передаем координаты как просто одной точки так и можно нескольких: scatte
() координаты (x, y) нужной точки, и функция нанесет эти 
значения на диаграмму:

fig,ax=plt.subplots()
ax.scatter(2,4) <--- одна точка на пересечении
ax.scatter(2,4бы=200)<---можно добавить размер шрифта и серию точек

x_values === [ 1, 2, 3, 4, 5]
y_values === [ 1, 4, 9, 16, 25]
fig, ax = plt.subplots()
ax.scatter(x_values, y_values, s=100)
subplot(nrows,ncols,index)
subplot(1,3,1)
subplot(1,3,2)
subplot(1,3,3)

nrows,ncols-число строк и столбцов
index= индекс существующих координатных осей

f,ax=plt.subplots(2,2)#по 2 графика в двух строках
plt.show()

РАЗНИЦА МЕЖДУ AX1.plot() И PLT.plot()                                               ╔═════════════╦═════════════╗
                                                                                    ║    ax[0,0]  ║    ax[0,1]  ║
ax.plot()=это объектно ориентированный подход, работа с конкретными                 ║             ║             ║
объектами которым уже дано название. более чистый и понятный код.                   ║             ║             ║
больше контроля над каждым графиком. лучше для сложных проектов                     ╠═════════════╬═════════════╣   
fig, ax = plt.subplots(2, 2)  # ax - это массивы,сами формы где находится           ║             ║             ║
график 2х2   fig-это само окно которое всплывает при запуске    кода                ║    ax[1,0]  ║    ax[1,1]  ║
                                                                                    ║             ║             ║
                                                                                    ╚═════════════╩═════════════╝                                        
                                                                       
                             
                                                                                   
                                                                                 
                                                                                   
                                                                                  
                                                                                    
                                                                                    
 

####Работаем с каждым графиком напрямую через его приобретенное название.............................................................

ax[0, 0].plot([1, 2, 3], [1, 2, 1])
ax[0, 0].set_title("График 1")
ax[0, 1].plot([1, 2, 3], [1, 4, 9])  
ax[0, 1].set_title("График 2")
ax[1, 0].plot([1, 2, 3], [2, 1, 2])
ax[1, 0].set_title("График 3")
ax[1, 1].plot([1, 2, 3], [3, 2, 3])
ax[1, 1].set_title("График 4")
plt.tight_layout()
plt.show()

plt.plot() ===State-based подход

просто обычная текущая работа с данным активным графиком переключаемся между графиками  с помощью plt.subplot(). 
простой но менее гибкий import matplotlib.pyplot as plt

Создаем сетку 2x2

plt.subplot(2, 2, 1)  # Переключаемся на 1-й график
plt.plot([1, 2, 3], [1, 2, 1])
plt.title("График 1")

plt.subplot(2, 2, 2)  # Переключаемся на 2-й график  
plt.plot([1, 2, 3], [1, 4, 9])
plt.title("График 2")

plt.subplot(2, 2, 3)  # Переключаемся на 3-й график
plt.plot([1, 2, 3], [2, 1, 2])
plt.title("График 3")

plt.subplot(2, 2, 4)  # Переключаемся на 4-й график
plt.plot([1, 2, 3], [3, 2, 3])
plt.title("График 4")

plt.tight_layout()
plt.show()


РАЗНИЦА МЕЖДУ add_axes([0.0,0,1.0,1.0])   и    add_subplot(1,3,1)

add_axes == создает рисунок на все открывшееся окно
add_subplot=== создает график с параметрами осей и графиком в окне 
граничные значения благодаря которым можно задавать для оси X и Y
Управление положением меток на координатах оси x и y
для этого в пакете matplotlib есть две функции:

set_major_locator()  И   set_minor_locator()

В качестве аргумента в этих функциях выступает класс matplotlib.ticker
Locator

from matplotlib.ticker import NullLocator

...................
.................
...............

lc=NullLocator()
ax.grid()
ax.xaxis.set_major_locator(lc)

                                ИЛИ чтобы не создавать объект класса

ax.xaxis.set_major_locator(NullLocator())                

xaxis= объект оси X.
set_major_locator(lc)=мы вызываем эту функцию для объекта xaxis  и в
качестве аргумента выступает экземпляр класса NullLocator -lc
NullLocator- используется для сокрытия меток по выбранной оси(X xaxis или Y
yaxis) то есть будут горизонтальные линии
Если ввести:
ax.yaxis.set_major_locator(lc)
                                ИЛИ чтобы не создавать объект класса

ax.yaxis.set_major_locator(NullLocator())                               
то на графике будут вертикальные линии


MultipleLocator класс
ax.xaxis.set_major_locator(MultipleLocator(base=1))
ax.grid()
plt.show()


MultipleLocator = он позволяет задавать шаг между рисочками на оси координат и для этого используется параметр base к которому приравнивается значение
точнее сколько отрезов мы хотим видеть на оси 
(MultipleLocator(base=1)) === значит цифры будут выставляться на 1-цу больше
то есть 1,2,3,.......


IndexLocator
работает почти так же как MultipleLocator,но производит отсчет рисок не от 0 а от наименьших значений данного графика.
и в самом конструкторе этого класса прописывается 2 параметра: base и offset.

base=шаг между рисками
offset= смещение относительно наименьшего значения.
Позиция риски = минимальное_значение + offset + n × base
N = 0, 1, 2, 3...

Для примера (min=2, base=2, offset=1):

Первая риска: 2 + 1 + 0×2 = 3
Вторая риска: 2 + 1 + 1×2 = 5
Третья риска: 2 + 1 + 2×2 = 7

Положительный offset сдвигает вправо, отрицательный - влево относительно минимального значения данных
ax.grid()
ax.xaxis.set_major_locator(IndexLocator(base=0.5,offset=0))
plt.show()
"""то есть мы на графике хотим видеть синусоиду в интервале от pi/2 строка 7 до pi с шагом 0.1.   
для локатора Indexlocator с параметро шага =0.5 и смещение 0 то есть мы продвигаемся от наименьшего значения ,от значения pi/2 и с шагом 0.5 
на графике будет видно что он начинается с -1.57, то есть половина pi. и шаг 0.5 по оси X.

-1.57-1.07-0.57-0.7........."""


ЛОГАРИФМИЧЕСКИЕ МАСШТАБЫ ИЗМЕНЕНИЯ ШАГА ПО ОСИ КООРДИНАТ

Некоторые детали нельзя рассмотреть в линейном масштабе,и при особенных графиках ,изменения которые должны отображаться на осях,должны принимать
особый алгоритм последовательностей. это делается с помощью функции 'semilog()'
 
ПРИМЕР:

ax.semilog(x,np.sinc(x) * np.exp(-np.abs(x/10))) 
ax.set_xscale()                                                                                             
                    
                    log - логарифмический масштаб
                    'symlog'- комбинация линейного и логарифмического масштаба. то есть в указанных пределах используем линейный а в остальной
области логарифмический. по умолчанию в этих параметрах используется параметр  10 как шаг по оси. то есть будет 10,20,30 и так далее. но чтобы указать свое основание,нужно добавить base=5(к примеру нужен шаг 5)

ВОТ ТАК:

ax.set_xscale('log', base=5)
в функцию xscale или yscale...............
SYMLOG --- ПАРАМЕТР
как понять где используется линейный а где алгорифмический при использовании этой функции???
при выборе оси(yscale или xscale), нужно после прописать linthresh и присвоить ему значение. это значение будет отображать значение как положительного так и отрицательного промежутка. если мы тут присваиваем 2,то линейный будет от -2 и до 2. если 5--то это будет от -5 и до 5
ax.set_yscale('symlog',linthresh=2)


ОТОБРАЖЕНИЕ СЕТКИ ЧЕРЕЗ minorticks_on()

мы подключаем minorticks_on()-- для координатных осей где будет отображатся минорная сетка  - -----> plt.minorticks() ---через plt
а потом вызываем метод grid(где и будем прописывать все параметры)

ВОТ ТАК:

plt.minorticks_on()
plt.grid( which='major', color='#444', linewidth = 1 )
plt.grid( which='minor', color='#aaa', ls = ':')
plt.show()

#which -- указывает какую сетку будем подключать major\minor?
#linewidth---- какая толщина ветки
 
У каждой оси определяется следующие элементы:
    title--сам заголовок для оси
    xlabel,ylabel----подпись для каждой оси
    text---произвольная информация в области осей
    annotate----сами аннотации(текст с указателем)

Для окна в целом:
        subtitle---заголовок для фигуры
        figtext----размещение произвольной текстовой информации в области окна




        fig =plt.figure(figsize=(7,4))#создаем окно
        ax=fig.add_subplot()# в axдобавляем координатную ось
        plt.figtext(0.05,0.6,'Текст в области окна')#размещаем текств области окна и        координаты нужно указывать в пределвх 1,потому что 1- это вся ширина
        fig.suptitle('Заголовок')#subtitle---исключительно для заголовка метод
        ax.set_xlabel('Ox')#подпись для оси X через set_label
        ax.set_ylabel('Yx')#подпись для оси Y через set_label
        ax.text(0.5,0.1,'Произвольный текст в координатных осях')
        ax.annotate('Аннотация', xy=(0.2, 0.4), xytext=(0.6, 0.7), 
                    arrowprops={'facecolor': 'gray', 'shrink': 0.1})
        plt.show()

в ax.text----- размещаем текст в области системы координат. то есть те координаты которые мы записали(0.05 и 0.1)-это координаты 
которые прокладываются по отношению уже имеющихся координат осей
annotate--- создает аннотацию----->подробней
Аннотация -- это просто какая нибудь подпись для элемента графика

Метод                              | Описание                             | Пример кода
-----------------------------------|--------------------------------------|----------------------------------------
set_text()                         | Устанавливает текстовую строку       | text_obj.set_text("Новый текст")
set_color() / set_c()              | Устанавливает цвет текста            | text_obj.set_color('red')<br>text_obj.set_c('#FF5733')
set_fontsize() / set_size()        | Устанавливает размер шрифта          | text_obj.set_fontsize(14)<br>text_obj.set_size('large')
set_fontfamily() / set_family()    | Устанавливает семейство шрифтов      | text_obj.set_fontfamily('serif')<br>text_obj.set_family('Arial')
set_fontweight() / set_weight()    | Устанавливает насыщенность шрифта    | text_obj.set_fontweight('bold')<br>text_obj.set_weight(700)
set_fontstyle() / set_style()      | Устанавливает стиль шрифта           | text_obj.set_fontstyle('italic')<br>text_obj.set_style('oblique')
set_position()                     | Устанавливает позицию (x, y) текста  | text_obj.set_position((0.5, 0.5))
set_horizontalalignment()/set_ha() | Горизонтальное выравнивание          | text_obj.set_ha('center')<br>text_obj.set_horizontalalignment('left')
set_verticalalignment() / set_va() | Вертикальное выравнивание            | text_obj.set_va('top')<br>text_obj.set_verticalalignment('center')
set_rotation()                     | Устанавливает угол поворота текста   | text_obj.set_rotation(45)<br>text_obj.set_rotation('vertical')
set_bbox()                         | Добавляет bounding box вокруг текста | text_obj.set_bbox(dict(facecolor='yellow', alpha=0.5))
set_alpha()                        | Устанавливает прозрачность           | text_obj.set_alpha(0.7)
set_backgroundcolor()              | Устанавливает цвет фона              | text_obj.set_backgroundcolor('lightblue')
set_usetex()                       | Включает/выключает рендеринг LaTeX   | text_obj.set_usetex(True)
set_visible()                      | Показывает/скрывает текст            | text_obj.set_visible(False)
get_text()                         | Возвращает текстовую строку          | current_text = text_obj.get_text()
get_color()                        | Возвращает цвет текста               | color = text_obj.get_color()
get_fontsize()                     | Возвращает размер шрифта             | size = text_obj.get_fontsize()
get_position()                     | Возвращает позицию (x, y)            | x, y = text_obj.get_position()
get_window_extent()                | Возвращает bounding box текста       | bbox = text_obj.get_window_extent()




LEGEND

когда у нас есть графики и нам нужно добавить краткое описание к каждому графику,то нам нужно:
1) вызвать метод который называется legeng() для нашей координатной оси ,например (ax), вот так:

ax.legend()
если мы вызываем legend(),то метод label уже не нужен.
legend(['1','2']) # тут мы даем название графикам в окне 1 и 2.

2) вызвать именованный параметр label там где определяем параметры графика на оси и присвоить ему название линии к которой хотим видеть описание,вот так ---->
ax.plot(np.arange(0,5,0.5), label='line 1')#первый график
ax.plot(np.arange(0,5,0.5), label='line 2')#второй график

можно добавить стиль этих линий,например чтобы они были пунктирными или еще какими-то,вот так

ax.plot(np.arange(0,5,0.5),'--o' label='line 1')


мы также можем указать в каком углу мы хотим видеть окно с подписью графиков с помощью переменной loc, мы ее прописываем в ax.legend .............  --->вот так

ax.legend(loc='upper right')

Параметр loc может принимать такие значения:

'best'
'upper right'
'upper left'
'lower right'
'right'
'center left'
'center right'
'lower center'
'upper center'
'center'

в качестве альтернативы если мы хотим задать отображение легенды через оси координат,то нужно использовать bbox_to_anchor=() ----> например
bbox_to_anchor=(0.5,0.7) от 0 и до 1 максимум

ax.legend(bbox_to_anchor=(1.05, 1), loc='upper left')  # справа от графика
ax.legend(bbox_to_anchor=(0.5, -0.1), loc='upper center')  # под графиком
ax.legend(bbox_to_anchor=(0, 0, 1, 1), loc='center')  # на весь график

 
Почему именно такие числа в bbox_to_anchor:

(0, 0) = левый нижний угол графика
(1, 1) = правый верхний угол графика
(1.05, 1) = чуть-чуть ПРАВЕЕ правого верхнего угла
(0, 1.05) = чуть-чуть ВЫШЕ левого верхнего угла



loc и bbox_to_anchor  ----->
ax.legend(bbox_to_anchor=(1.05, 1), loc='upper left')
если мы хотим указать параметр который больше 1,потому что всего масштаю окна расчитан с 0 и до 1.
и если мы пишем 1.2--- это значит что мы на 20% возвышаем над допустимыми размерами и будет ошибка.мы будем находится уже за пределами графика,вот!  
чтобы отображение сработало так как нужно,нужно  использовать bbox_to_anchor  для того чтобы табличка не была не сбита со своих надстроек и была действительно в том расположении где мы прописали,но тогда,нужно:
       
    1) стиль:
        ax.legend(bbox_to anchor=(1.1,0.5))
        и обязательно после ------>
        plt.tight_layout()

    2)стиль: ТОЛЬКО ПРИ СОХРАНЕНИИ В PNG ФАЙЛ
        ax.legend(bbox_to_anchor=(1.2,0,5))
        plt.savefig('plot.png',bbox_inches='tight') ----


БЕЗОПАСНЫЕ варианты (работают всегда):

ax.legend(loc='best')                   # внутри графика
ax.legend(bbox_to_anchor=(0.5, 0.5))    # внутри графика

ОПАСНЫЕ варианты (требуют tight_layout): бОЛЬШЕ 1

ax.legend(bbox_to_anchor=(1.05, 0.5))   # чуть справа
ax.legend(bbox_to_anchor=(1.2, 0.5))    # дальше справа
ax.legend(bbox_to_anchor=(-0.1, 0.5))   # слева

ЗАЩИТА от обрезания:

plt.tight_layout()  # ДОБАВЛЯЕМ ПОСЛЕ ЛЮБОГО "опасного" bbox_to_anchor


Комбинация loc и bbox_to_anchor

ax.legend(bbox_to_anchor=(0.5, 0.5), loc='center')

для оформления рамки в окне с легендой:

fig = plt.figure(figsize=(7,4))
ax = fig.add_subplot()

line1, = ax.plot(np.arange(0,5,0.25), '--o', label='Линия 1')
line2, = ax.plot(np.arange(0,10,0.5), ':s', label='Линия 2')

настройка легенды:

legend = ax.legend(
    loc='upper right',
    fontsize=12,                   #размер шрифта
    title='Легенда',               #заголовок легенды
    title_fontsize=14,             #размер шрифта заголовка
    frameon=True,                  #показывать рамку
    fancybox=True,                 #скругленные углы
    shadow=True,                   #тень
    facecolor='white',             #цвет фона
    edgecolor='black',             #цвет рамки
    framealpha=0.9,                #прозрачность фона
    ncol=1,                        #количество столбцов
    handlelength=1.5,              #длина элементов
    handletextpad=0.5,             #отступ текста
    borderpad=1,                   #отступ от границ
    labelspacing=0.5               #расстояние между метками
)


КАСТОМНЫЕ ЛЕГЕНДЫ

from matplotlib.lines import Line2D

custom_lines = [
    Line2D([0], [0], color='blue', lw=2, linestyle='--'),
    Line2D([0], [0], color='red', lw=2, marker='o')
]

ax.legend(custom_lines, ['пунктирная', 'с маркерами'])


get_frame()----- для работы только с рамкой легенды,то есть толщина,цвет,цвет фона рамки,прозрачность,

legend = ax.legend()
frame = legend.get_frame()


Список методов get_frame():

frame.set_facecolor('lightblue')    #цвет фона
frame.set_edgecolor('red')          #цвет рамки
frame.set_linewidth(3)              #толщина рамки
frame.set_linestyle('--')           #стиль линии рамки
frame.set_alpha(0.7)                #прозрачность
frame.set_boxstyle('round,pad=0.5') #стиль рамки
frame.set_fill(True)                #заливка включена/выключена
frame.set_capstyle('round')         #стиль концов линий
frame.set_joinstyle('round')        #стиль соединений углов

Дополнительные настройки:

                    frame.set_hatch('//')               #штриховка
                    frame.set_zorder(10)                #порядок отображения


c get_title()------- 

legend = ax.legend(title='Мой заголовок')
title = legend.get_title()

Методы get_title():
title.set_color('red')              #цвет текста
title.set_fontsize(16)              #размер шрифта
title.set_fontweight('bold')        #жирность
title.set_fontstyle('italic')       #наклон
title.set_family('serif')           #семейство шрифтов
title.set_ha('center')              #горизонтальное выравнивание
title.set_va('top')                 #вертикальное выравнивание
title.set_rotation(0)               #поворот текста
title.set_backgroundcolor('yellow') #цвет фона текста
title.set_alpha(0.8)                #прозрачность

для комплексной настройки - только словарями:

title.set_fontproperties({
    'family': 'Arial',
    'size': 14,
    'weight': 'bold',
    'style': 'italic'
})



Ступенчатые, стековые, stem и точечные графики.

step_stem_scatter_plots.py

import numpy as np
import matplotlib.pyplot as plt


#создаем фигуру и координатные оси
fig=plt.figure(figsize=(4,4))
ax=fig.add_subplot()



x=np.arange(0,10)     #формируем координату X от 0 и до 9,
ax.step(x,x, '-go')   #отображаем этот график как по X так и по Y (от 0 и до 9)
#-go----тип линии,так же мы можем указать как 3-ю переменную :тип линии,цвет и маркер
#ax.step(x,x,where='post')   #здесь мы указываем post-как инверсную картину,так же можно прописать 'mid' а по умолчанию используется метод 'pre'


#чтобы передать параметры для двоих графиков:
-----------------------------------------------------------------
|#ax.step(x,x,where='post', |  np.cos(x), '--x', where='mid')   |
|    #первый график         |          второй график            |

ax.grid()
plt.show()


#стековый график
#Yi(x) = y1(x) + y2(x) + ... + yi(x)


fig=plt.figure(figsize=(4,4))
ax=fig.add_subplot()
x=np.arange(-2,2,0.1)# множество значений по оси X
y1=np.array([-y**2 for y in x]) + 8  #множество значений по оси Y
y2=np.array([-y**2 for y in x]) + 8  #множество значений по оси Y
y3=np.array([-y**2 for y in x]) + 8  #множество значений по оси Y
Минус возле Y- значит ветки идут вниз



ax.stackplot(x,y1,y2,y3)

ax.grid()
plt.show()


2D графики

(x[i,j], y[i,j])

i j --номера строки в сетке
x[i,j]--вместе адрес конкретной ячейки в сетке

X-- смещение влево или в право
Y-- смещение вверх или вниз


   x    x    x    x
-------------------------
y| •  |  •  |  •  |  •  | → x[0,0], y[0,0] = (0,0)
y| •  |  •  |  •  |  •  | → x[0,1], y[0,1] = (1,0)  
y| •  |  •  |  •  |  •  | → x[0,2], y[0,2] = (2,0)
y| •  |  •  |  •  |  •  | → x[0,3], y[0,3] = (3,0)
-------------------------

для регулярных сеток сначала определяется одномерные списки:
x=[1,2,3]
y=[2,5,6,8]

ПОСКОЛЬКУ Х идет вертикально а  y горизонтально,то с такими данными получаем следующие xgrid и ygrid для каждой точки сетки:

xgrid:            ygrid: 
    [1, 2, 3]        [2, 2, 2]
    [1, 2, 3]        [5, 5, 5] 
    [1, 2, 3]        [6, 6, 6]
    [1, 2, 3]        [8, 8, 8]

Для одномерных массивов -- они могут формировать только регулярные сетки,а если нужно например создать гексагональную сетку (как соты(это на тему пчелок,меда)),в такой сетке уже не получится обойтись.Это также используется обойтись одномерными массивами а нужно будет прописывать узлы этой сетки двумерными массивами и для этого и отображено трехмерные графики через двумерные массивы как xgrid и ygrid



чтобы  можно было получить двумерные массивы,есть функция meshgrid()

xgrid,ygrid = mashgrid(x,y). на её вход подается одномерные массивы X и Y.а на выходе получаем двумерный массивы xgrid и ygrid


индексы i и j- они будут перебирать
все классы для работы с 3D - находятся в модуле mplot3d


 STEM-график


fig=plt.figure(figsize=(4,4))
ax=fig.add_subplot()
x=np.arange(-2,2,0.1)# множество значений по оси X


np.arange(-np.pi,np.pi,0.3)
ax.stem(x, np.cos(x))
#мы отображаем ксинусоиду с помощью STEM графика множество точек для функции косинуса на графике,они соеденены линией с базовым уровнем,котоырй находится в точке 0 по оси Y. и этот базовый уровень можно изменить с помощью параметра bottom=нужный нам уровень,например bottom=0.5(последний параметр)
тип и цвет(3й параметр), к примеру '--r'
ax.stem(x, np.cos(x),'--r',bottom=0.)

ax.grid()
plt.show()


Точечный график
                   с помощью функции scater()



fig=plt.figure(figsize=(4,4))
ax=fig.add_subplot()

x=np.random.normal(0,2,500)
y=np.random.normal(0,2,500)
ax.scatter(x,y)
#функция с параметрами:
ax.scatter(x,y,s=50,c='g',linewidths=1,marker='s',edgecolors='r')
s=50, === размер 0
c='g',===цвет зеленый
linewidths=1, ===толщина линии 1
marker='s' ===маркер s
,edgecolors='r'===цвет граней точек красный
ax.grid()
plt.show()



Гистограммы, столбчатые и круговые диаграммы

fig=plt.figure(figsize=(6,4))
ax=fig.add_subplot()
y=np.random.normal(0,2,500)#здесь мы генерируем 500 случайных величин,которые распределены по normal(нормальному закону распределения) с 0 средним        
#ожиданием и мера разброса -2
ax.hist(y)

потом мы переходим к оси x которая отображает гистограмму(hist функция), тех случайных величин которые мы задали
hist функция делает сама разбиение по диапазонам

если мы хотим изменить коллдичество интервалов,например 50,то в обьявлении функции hist --->ax.hist() нужно прописать вторым параметром,следующее --->
ax.hist(y,50) #50--- 
весь интервал который принимает величина Y, от миниального до максимального разбивается на 50 равных интервалов и потом подсчитывается сколько случайных величин попало в тот или иной интервал и строится график

ax.grid()
plt.show()

но такой график можно еще построить с поощью функции bar() вот таким образом-------------->>>>>

fig=plt.figure(figsize=(6,4))
ax=fig.add_subplot()

x=[f'H{i+1}' for i in range(10)] #----список отметок для каждого столбца
y=np.random.randint(1,5,len(x))#------значение высот для соответствующего столбца
и колличество столюцов должно совпадать с колличеством отметок
ax.bar(x,y)#------ а в функции bar мы получаем сформированную столбчатую диаграмму
по вертикали  откладываются столбцы и по X оси будет видно отметки H1 H2 H3........до H10
с помощью функции bar() можно строить произвольные столбчатые диаграммы
ax.grid()
plt.show()




Гистограммы с bar()функцией

функция bar()---отображает  столбцы относительно оси Х.

если нужно чтобы отображалось относительно оси Y,то используем функцию barh()
fig=plt.figure(figsize=(6,4))
ax=fig.add_subplot()
y=np.random.normal(0,2,500)#множество значений Y
x=np.linspace(np.min(y),np.max(y), 10)#множество значений X.зжесь мы разбиваем весь интервал который принимает Y от min и до  max значений на 10 равных интервалов.
bars=[len(y[np.bitwise_and(y >=x[i], y < x[i+1]>)]) for i in range(len(x)-1)] #тут мы смотрим сколько случайных величин попадает в тот или иной интервал X ------->

for i in range(len(x)-1) -----здесь перебираем все значения Х до предпоследнего. и смотрим сколько значений Y (y попадает в интервал -> 

y >=x[i], y < x[i+1]>  --->

от x[i] и до x[i+1]

и все это подсчитывает функция Len и все это формируется в функцию bars

ПОТОМ

x=[f'H{i+1}' for i in range(10)]
y=np.random.normal(0,2,500)
ax.bar(range(len(x)-1),bars)
range(len(x)-1) ----мы не можем просто вместо всей этой функции range указать x ,потому что x уже больше 1 у нас
мы берем итератор range который генерирует  числа от 0 и до размера вектора x-1



КРУГОВЫЕ ДИАГРАММЫ 
 
                   

для изображения доли чего либо в круговых диаграммах- нужна функция pie.

import numpy as np
import matplotlib.pyplot as plt

fig=plt.figure(figsize=(6,4))
 ax=fig.add_subplot()

vals=[10,40,23,20,7] # всего 100=100%
labels=['Toyota','BMW','Lexus','Audi','Mercedes']

vals и labels===
vals= просто набор меток,множество чисел,долей,то есть каждое число это доля каждого наименования в переменной labels  и labels - наборзначений для этих меток

#ax.pie(vals,labels=labels)
#а можно и так
exp=(0.1,0.2,0,0,0)
ax.pie(vals,labels=labels,autopct='%.2f',explode=exp,shadow-True)
#здесь мы говорим,что передаем все параметры переменной vals, все метки в labels, в каком формате будут отображаться значение долей,с точностью до сотых с помощью autopct, explode-- говорит какие доли будут выдвинуты из круговой диаграммы и указываем на какую величину они будут выдвинуты с помощью переменной wxp, и с shadow=True --- показываем тень с которой они будут отображаться и отображаем их в качестве круговой диаграммы с функцией pie

ax,grid()
plt.show()


 Параметры функции PIE



Параметр       | Тип              | Значение по умолчанию| Описание                                             | Пример
x              | array-like       | -                    | Обязательный. Массив числовых значений и              | [30, 25, 45]
                                                           Сумма значений автоматически  к 100%.

explode        | array-like       | None                 | Массив значений, указывающих насколько                | [0.1, 0, 0]
                                                           выдвинуть каждый сегмент из центра.

labels         | list of str      | None                 | Список названий для каждого сегмента.                 | ['A', 'B', 'C']
colors         | array-like       | None                 | Список цветов для сегментов.                          | ['red', 'blue', 'green']
autopct        | str или callable | None                 | Формат отображения процентов внутри сегментов.        | '%1.1f%%', lambda p: f'{p:.1f}%'
pctdistance    | float            | 0.6                  | Расстояние текста процентов от центра (0-1).          | 0.8
shadow         | bool             | False                | Добавляет тень под диаграммой.                        | True
labeldistance  | float или None   | 1.1                  | Расстояние подписей сегментов от центра.              | 1.1
startangle     | float            | 0                    | Угол начала первого сегмента (в градусах, 0 = справа).| 90
radius         | float            | 1                    | Радиус круговой диаграммы.                            | 1.2

wedgeprops     | dict             | None                 | Словарь свойств для сегментов (граница, прозрачность и т..).                                                                                                            | {'edgecolor': 'black', 'linewidth': 2}
textprops      | dict             | None                 | Свойства для текста (цвет, размер шрифта и т.д.).     | {'color': 'white', 'fontsize': 14}
center         | tuple            | (0, 0)               | Координаты центра диаграммы (x, y).                   | (0.5, 0.5)
frame          | bool             | False                | Отображать рамку вокруг диаграммы.                    | True
rotatelabels   | bool             | False                | Поворачивает подписи к соответствующим сегментам.     | True
normalize      | bool             | True                 | Автоматическая нормализация значений к 100%.          | False
counterclock	bool	            True	               Направление сегментов (True - против часовой)	     | False


Трехмерные графики

mpl_toolkits.mplot3d, который импортируется
from mplot_toolkits.mplot3d import Axes3D


import numpy as np
import matplotlib.pyplot as plt
from mplot_toolkits.mplot3d import Axes3D

fir=ply.figure(figsize(7,4))
#ax_3d= Axes3D(fig)
#здесь создаем трехмерную ось с помощью класса Axes3D
ax_3d=fig.add_subplot(projection='3d')
c add_subplot создаем ось координат и с помощью параметра projection указываем что эта ось будет трезмерной-3D. но тогда импортировать модуль Axes3D не обязательно
plt.show()


plot()--- линейный 2d график в трех измерениях
step()--- ступенчатый 2d график в трех измерениях
scatter()--- точечный график 3d

plot_wireframe()- построение каркасной поверхности(сетки) в 3d
plot_surface()-- построение непрерывной 3D поверхности --поверхности


Параметры wireframe и surface: 
x,y,z ====2d массивы для построения трехмерных графиков
rstride,cstride ===== величина шага, с которым будут выбирая элементы из массивов x y
color ======цвет графика
cmap==== цветовая карта графика
import numpy as np
import matplotlib.pyplot as plt

fig=plt.figure(figsize(7,4))
ax_3d=fig.add_subplot(projection='3d')

x=np.linspace(0,10,50)
#координата x которая будет менятся от 0 до 10 и будет разбиватся на 50 равных поддиапазонов,отрезков 
z=np.cos(x)
#а Z - вычисляет ксинусоиду на основе предоставленых значений X
И чтобы построить этот график в трехмерных координатных осях,достаточно вызвать метод plot но указать три параметра первые 2=это координаты в прлоскости X и Y,а третья -Z-- это значение по вертикали
ax_3d.plot(x, x, z)
plt.show() 

    

Для наименования осей используем set_label()
ax_3d.set_xlabel('X')
ax_3d.set_ylabel('Y')
ax_3d.set_zlabel('Z')


